class Gallery {
  String url;
  String title;
  String image_count;
  String cata;
  List tags;
  String rating;

  Gallery(this.url, this.title, this.image_count, this.cata, this.tags, this.rating);
}